<?xml version="1.0" encoding="utf-8" ?>
<uranium:UraniumContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:uranium="clr-namespace:UraniumUI.Pages;assembly=UraniumUI"
             xmlns:material="clr-namespace:UraniumUI.Material.Attachments;assembly=UraniumUI.Material"
             xmlns:u="clr-namespace:UraniumUI;assembly=UraniumUI"
             x:Class="UraniumApp.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <!--<Image

                Source="dotnet_bot.png"
                SemanticProperties.Description="Cute dot net bot waving hi to you!"
                HeightRequest="200"
                HorizontalOptions="Center" />

            <Label
                Text="Hello, World!"
                SemanticProperties.HeadingLevel="Level1"
                FontSize="32"
                HorizontalOptions="Center" />

            <Label
                Text="Welcome to .NET Multi-platform App UI"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I"
                FontSize="18"
                HorizontalOptions="Center" />-->

            <Button
                x:Name="CounterBtn" StyleClass="ElevatedButton"
                Text="Click me"
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center">
                <Button.Shadow>
                    <Shadow Brush="{StaticResource ShadowBrush}" Opacity="0.2" Offset="2,2" Radius="4" />
                </Button.Shadow>
            </Button>

            <Frame Padding="0,10" HorizontalOptions="Start" BackgroundColor="{StaticResource Primary}">

                <Grid ColumnDefinitions="Auto,Auto" RowDefinitions="*,*,*,*,*" RowSpacing="10" ColumnSpacing="20">
                    <Grid.Resources>
                        <Style TargetType="Label">
                            <Setter Property="VerticalOptions" Value="Center"/>
                            <Setter Property="HorizontalOptions" Value="Center"/>
                            <Setter Property="Margin" Value="20"/>
                        </Style>
                        <Style TargetType="BoxView">
                            <Setter Property="Color" Value="{StaticResource Gray100}" />
                            <Setter Property="HeightRequest" Value="2" />
                            <Setter Property="CornerRadius" Value="1" />
                        </Style>
                    </Grid.Resources>

                    <Label Grid.Column="0" Text="Header A" FontAttributes="Bold" />
                    <Label Grid.Column="1" Text="Header B" FontAttributes="Bold" />

                    <BoxView Grid.Row="1" Grid.ColumnSpan="2" HorizontalOptions="Fill"/>



                    <Label Grid.Row="2" Grid.Column="0" Text="Data A.1" />
                    <Label Grid.Row="2" Grid.Column="1" Text="Data B.1" />

                    <BoxView Grid.Row="3" Grid.ColumnSpan="2" HorizontalOptions="Fill"/>

                    <Label Grid.Row="4" Grid.Column="0" Text="Data A.2" />
                    <Label Grid.Row="4" Grid.Column="1" Text="Data B.2" />

                </Grid>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</uranium:UraniumContentPage>
